<DocsHero
  @logo='ember'
  @slimHeading='Ember'
  @strongHeading='MaplibreGL'
  @byline='Fast, expressive MaplibreGL maps'
/>

<MaplibreGl as |map|>
  <map.source @options={{hash type='geojson' data=this.wanderDrone}} as |source|>
    <source.layer @layer={{hash type='symbol' layout=(hash icon-image='rocket-15')}} />
  </map.source>

  {{#if this.curLocation}}
    <map.marker @lngLat={{this.curLocation}} as |marker|>
      <marker.popup>
        This is where you are!
      </marker.popup>
    </map.marker>

    {{map.call 'easeTo' (hash center=this.curLocation)}}
  {{/if}}

  {{map.on 'click' this.onClick}}
</MaplibreGl>
